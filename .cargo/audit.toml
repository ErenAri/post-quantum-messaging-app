[advisories]
ignore = [
  # Transitively pulled by sqlx::Any through sqlx-mysql, but pqmsg-server only
  # supports SQLite and PostgreSQL in the documented deployment contract.
  "RUSTSEC-2023-0071",
  # Transitively inherited through upstream deps (tokio-tungstenite/sqlx/sentry
  # families) and tracked until patched upstream.
  "RUSTSEC-2026-0097",
]
